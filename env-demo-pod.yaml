apiVersion: v1
kind: Pod
metadata:
  name: env-demo-pod
spec:
  containers:
  - name: ubuntu
    image: ubuntu
    env:
    - name: GREETING
      value: "NGINX"
    - name: MESSAGE
      value: "PORT=80"
    command:
    - sh
    - -c
    - "sleep 10000"

